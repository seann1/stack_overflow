<% @questions.each do |q| %>
  <h3><%= link_to "#{q.title}", question_path(q.id) %></h3>
<% end %>

<br />

<% if current_user != nil %>
  <%= link_to 'Have a question?', new_question_path(current_user), id: 'new_link',  remote: true %>
<% end %>
