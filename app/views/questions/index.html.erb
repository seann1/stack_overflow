<% @questions.each do |q| %>
  <h4><%= link_to "#{q.title}", question_path(q.id) %></h4>
<% end %>
  <div id="questions">
  </div>

<br />
<div id="forms">
<% if current_user != nil %>
  <%= link_to 'New Question', new_question_path, id: 'new_link',  remote: true %>
<% end %>
</div>
