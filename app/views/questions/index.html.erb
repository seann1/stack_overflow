<% if current_user == nil %>
<div class="row">
<p>log in with test@test.com</p>
<p>Password: password </p>
</row>
<% end %>
<% @questions.each do |q| %>
<div class="row">
  <h4 class="question_list small-3 small-centered columns"><%= link_to "#{q.title}", question_path(q.id) %></h4>
</row>
<% end %>
  <div id="questions">
  </div>

<br />
<div id="forms">
  <div class="row">
<% if current_user != nil %>
  <%= link_to 'New Question', new_question_path, id: 'new_link', class: "button", remote: true %>
<% end %>
</div>
</div>
